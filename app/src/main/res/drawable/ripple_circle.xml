<?xml version="1.0" encoding="utf-8"?>
<!--
  圆形水波纹（ADR-086）。

  ⚠️⚠️ 为什么需要它（用户反馈「点击效果蓝色的圈位置不对 左右都不对」）：
     · 箭头按钮：设了 insetLeft/Right=0 + padding=0 后，
       ripple 的边界变成整个 40dp 方块 —— 而图标只有 22dp，
       视觉上"蓝圈比图标大，且看着偏"。
     · 「第 N 周」用 ?attr/selectableItemBackground（无边界遮罩），
       ripple 会溢满整个 TextView 区域（含 8dp padding），
       点击时蓝圈超出文字范围。
     · 「回到本周」是 TextButton，本身带圆角 ripple，
       但与左右两个箭头的形态不一致。

  ✅ 统一方案：**所有点击项都用同一个圆形 ripple**，
     mask 用 <oval>（圆形），半径由控件尺寸决定。
     · 40dp 的箭头按钮 → 直径 40dp 的圆（恰好裹住 22dp 图标）
     · 「第 N 周」与「回到本周」→ 圆角矩形（按钮形态，符合 M3）

  ⚠️ colorControlHighlight 用 12% 透明度：
     M3 标准 ripple 透明度，太深会显脏。
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="#FFFFFFFF" />
        </shape>
    </item>
</ripple>