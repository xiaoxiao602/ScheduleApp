<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--
      ADR-092：触感反馈。
      ⚠️ 只在用户把「震动强度」调到 <100% 时才真正用到
         （此时用 Vibrator 施加振幅缩放）。保持 100% 时完全走
         View.performHapticFeedback，不需要此权限。
      ⚠️ 这是普通权限（normal），安装时不会弹窗，用户无感知。
    -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="false"
        android:label="广软课程表"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.ScheduleApp">

        <!-- 启动入口 = MainActivity（ADR-005）：先看数据，不弹登录 -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 登录页：从「设置」进入 -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- 二级页（ADR-006） -->
        <activity
            android:name=".ui.grade.GradeListActivity"
            android:exported="false" />

        <activity
            android:name=".ui.dock.DockTuningActivity"
            android:exported="false" />

        <activity
            android:name=".ui.haptic.HapticActivity"
            android:exported="false" />

        <activity
            android:name=".ui.color.CourseColorActivity"
            android:exported="false" />
        <activity
            android:name=".ui.exam.ExamListActivity"
            android:exported="false" />

    </application>

</manifest>
