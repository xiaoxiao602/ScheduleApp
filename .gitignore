# ===== Android / Gradle 构建产物 =====
*.iml
.gradle/
build/
app/build/
captures/
.externalNativeBuild/
.cxx/
# ⚠️ release/ 存放要分发的成品 APK，故不忽略（原为 release/）
*.aab
*.ap_
*.dex
*.class
output.json

# ⚠️ ADR-091：**允许**上传 APK（用户要求「apk 文件也上传」）
#    注：只上传 release/桌面的成品包，build/ 目录整个被忽略。
#    若将来 APK 体积过大（>50MB），GitHub 单文件上限 100MB。

# ===== 本机配置（含绝对路径，换机器会失效）=====
local.properties

# ===== 签名密钥（绝对不能上传）=====
*.jks
*.keystore
*.p12
*.pem
*.key
keystore.properties
signing.properties

# ===== IDE =====
.idea/
*.iws
*.ipr
.vscode/
.kotlin/

# ===== 操作系统 =====
.DS_Store
Thumbs.db
desktop.ini

# ===== 日志 / 临时文件 =====
*.log
*.bak
*.bak-*
*.tmp
*.orig
*.rej
hs_err_pid*

# ===== 编辑备份归档（开发期临时产物，不属于项目源码）=====
docs/_archived_schedule_view/
# ===== 教务对接技术文档（含 CAS 地址 / RSA 公钥 / 接口路径）=====
# ⚠️ 绝对禁止提交：这些细节被公开会带来风控与安全风险。
#    本地档案另存于项目外：C:\Users\34356\dev\Notes\ScheduleApp\
docs/应用与功能说明书.md
docs/教务*
docs/*对接*